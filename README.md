# Rule-Based-Chatbot
RuleBot is a lightweight command-line chatbot designed to demonstrate the fundamentals of rule-based natural language processing. It handles user inputs dynamically by stripping whitespace and ignoring letter casing, matching keywords to deliver predefined responses, and handling exit commands smoothly.
